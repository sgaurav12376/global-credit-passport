spring.application.name=register-service
server.port=8081

# DB (override with env in docker compose if you want)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://sachu01.c6reawuqi249.us-east-1.rds.amazonaws.com:5432/sachu}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:sachu123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# AWS / Cognito
aws.region=${AWS_REGION:us-east-1}
aws.cognito.userPoolId=${AWS_COGNITO_USERPOOL_ID:us-east-1_mPnp7FUWD}
aws.cognito.clientId=${AWS_COGNITO_APP_CLIENT_ID:4p8dsmf03m5hvpf1ta36q468ab}

# Logging (optional)
logging.level.com.global_credit_app=DEBUG
