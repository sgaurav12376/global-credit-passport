server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gcp
    username: gcp
    password: gcp
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
